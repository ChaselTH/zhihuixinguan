/* Minimal local HTML5 element shim for IE 8; no external network request. */
(function(d){var e='article aside footer header main nav section'.split(' '),i;for(i=0;i<e.length;i++){d.createElement(e[i]);}})(document);
