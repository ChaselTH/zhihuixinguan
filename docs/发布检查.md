# 智慧信管 0.3.0-rc.9 发布检查

2026-09-18，用户在 PR #11 合并后要求将安装包放入 U 盘。本次发布基于 main 的 FEEDBACK-007 合并提交 `1c822ed69822db0bd48bbf95ce39a7b6f4684805`；实际发布提交以安装目录内 `SOURCE_COMMIT` 为准。

## 交付要求

- 应用 `0.3.0-rc.9` / schema 7，包含三类统一表内填写、跨页私人草稿、紧凑差异编辑、支行统一修改记录和多支行整批提交；保留 rc.3～rc.8 全部现有能力。V001～V007 不改，无新增迁移。
- 从干净、已提交源码构建，离线依赖校验固定 SHA-256；不打包业务库、上传表、草稿、运行历史或 Git 历史。
- 两种架构继续使用已校验的 Microsoft JDK 21.0.12 Linux x86_64／aarch64 缓存，不在 Windows 执行 Linux Java。
- 安装目录 `zhihui-xinguan-offline-0.3.0-rc.9-kylin` 与 U 盘旧版本并存，不覆盖或删除旧包。拷贝后逐文件 SHA-256 核对。
- 私人初始化配置只显式放入私人包，空库才使用；升级保留已有账号及配置。整个私人包不得上传 GitHub 或公开转发。
- 不启用 GitHub CI；不重启用户当前 2874 服务，不访问其业务数据库。

## 本地验证

发布前必须执行，任一失败均不交付：

```text
node tools/build.mjs --test --offline
node tests/install-smoke.mjs
git diff --check
```

合并阶段已通过全量离线回归：WorkflowPlatform 241、Feedback007Regression 156、WorkflowRoutes 100、BusinessHttp 62、BusinessView 221、workflow-interactions 43；其他既有五角色 HTTP、解析／导入／导出、反馈日期、人员、维护、迁移套件均通过。详细用例及接口见 [维护修复说明](feedback007-维护修复.md)。

安装器模拟覆盖新装、私人初始化配置、升级保留数据及配置、完整旧版备份、迁移失败保护和校验失败。使用隔离目录及合成运行时，不能等同麒麟实机安装。

2026-09-18，更新 rc.9 版本及安装说明后重新执行上述三项，全部通过：BUILD_OK、INSTALL_SMOKE_OK；重点测试断言数与合并阶段一致。随后从同一代码的干净提交重新构建交付包，并由打包程序逐文件校验 U 盘副本。不启用云端 CI，不声明未经运行的检查通过。

## 升级及回退

先停止工作电脑旧服务，不卸载，再运行新包 install.sh，安装目标须为原安装目录。默认沿用 `ZhihuiXinguan-PR0`。全部原账号、数据、草稿、待办、审计及反馈截止设置随原数据目录保留；迁移先在暂存副本验证，成功才替换并保留完整备份。

schema 仍为 7，但新增的 BATCH_DIRECT 提交模式不能由 rc.8 及更早程序读取。产生新模式提交后不可只换旧二进制降级；回退须使用升级前完整备份，同时先保全升级后新数据。备份须包含整个 data 及 initial-password.key 和副本。

## 待现场验收

已使用隔离合成数据抽查 Chromium 的 1366／1920 布局及跨页保存→预览→提交。真实麒麟版本／CPU、Win7 实际 IE／文档模式、离线升级后的业务核对、生产数据量性能和行内网络策略仍需现场验收。

正式安全文案及其他行内数据模板待提供；正式推广前需确认传输保护。本包是联调测试版，不表示上述实机及安全事项已通过。
