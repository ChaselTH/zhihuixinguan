# 智慧信管 0.3.0-rc.2 发布检查

2026-09-17，用户授权复审合并 PR #10 并交付 U 盘测试包。功能审查基线 `dff545d96e21bb185decf7fba0e312b5bb8e9c14`；PR #10 合并点 `5e451cc5ea832f02b18dad6149ccce9983a095cb`。实际包与源码以随附 SOURCE_COMMIT 为准。本次发布收尾仅整理版本、安装说明及协作状态，不增加新业务需求。

## 交付规则

- 版本 0.3.0-rc.2 / schema 5。包含 V005 申请角色与临时密码密文字段；V001～V004 原脚本不改。
- 构建必须来自干净且已提交的合并源码，全量离线测试和安装器模拟检查出错不得交付。不使用旧版本断言总数冒充本次新增证据。
- GitHub 自动工作流已手动停用，源码移除 build.yml；没有把“未运行”写成“云端测试通过”，不更改无关仓库安全设置。
- 麒麟包使用与之前 PR0.7 一致的两个 Linux JDK 归档，打包时核验固定 SHA-256；应用和 U 盘副本逐文件核对。不是在开发电脑执行 Linux JDK，也不声称已完成麒麟兼容测试。
- 源码导出来自 Git 提交，仅补锁定 JAR 缓存，拒绝业务文件和运行数据；目录含校验清单。旧 U 盘文件不覆盖。
- 私人安装包允许显式带现有本地初始化配置，只对空库生效；源码副本和 GitHub 均不含该文件。

## 本地复现

```text
node tools/build.mjs --test --offline
node tests/install-smoke.mjs
```

上述命令已在功能审查基线及 rc.2 发布收尾时各执行一轮，均输出 BUILD_OK / INSTALL_SMOKE_OK。完整回归包含五角色权限、三表年月季度区间导出、持久化批量导入与确认、草稿与原子审批、正式筛选与流程提示、通知审计、并发幂等、整单失败回滚、旧库升级。测试使用虚构身份、业务行及工作簿。

PR #10 本次补丁证据：AccessPlatformTest 78 项、ImportHttpTest 55 项、ImportWorkbookTest 219 项断言通过；跨月两个完整工作簿经真实 HTTP 上传、确认、按月查询与导出正确，公共月份冲突时整批拒绝。四参数空角色申请在有／无管理员时拒绝。

维护人独立复现探针 9 项通过、0 项问题：原空角色、重复提示、待申请开户、最后管理员转机构、月份冲突、版本标记等检查通过；实际临时密码解密与主密钥丢失后的同备份恢复通过。

额外使用旧 rc.1 的实际 schema 4 程序生成合成库，由新程序升级为 V5，验证两个管理员账号密码、正式条数、反馈、历史月份、前导零客户编码及待审批申请保留；旧程序再次打开 V5 库明确拒绝。该验证不连接用户的 2874 实例。

安装模拟检查新装、旧配置及数据保留、升级备份、校验失败和迁移失败保护。已有现代浏览器 1366／1920 检查仅说明该环境布局可用，不代表真实旧 IE。

## 待用户现场验收

麒麟真实系统和 CPU 的断网安装、实际 Win7 IE／文档模式、旧安装升级后的业务核对、真实网络策略，以及生产数据体量下的性能和保留策略。本包供联调，不作为上述事项已通过的证明。正式安全文案、截止字段和新增行内数据模板仍待提供。
