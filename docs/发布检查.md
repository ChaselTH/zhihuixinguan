# 智慧信管 0.3.0-rc.6 发布检查

2026-09-18，用户要求将当前版本制作成 U 盘安装包。功能基线为 `dbaf12ce4bda47e03d5a60da2f670843a456d281`；本次收尾更新随包安装说明和验证记录，不增加业务功能。实际打包提交以安装目录内 `SOURCE_COMMIT` 为准。

## 交付要求

- 应用 `0.3.0-rc.6` / schema 6，包含 rc.3 导入／风险展示调整、rc.4 按期次反馈日期、rc.5 日期最早明天限制及 rc.6 人员删除入口；V001～V006 原迁移不改。
- 从干净、已提交的源码重新构建，执行全量离线测试与安装器模拟，失败不交付。
- 使用既有 PR0.7 的 Microsoft JDK 21.0.12 Linux x86_64／aarch64 归档，固定 SHA-256 校验；应用和 U 盘副本逐文件校验。不在 Windows 执行 Linux Java，不把归档校验当成麒麟实测。
- 安装目录与所有旧 U 盘版本并存，不覆盖旧包。只复制本次明确目标，不清理 U 盘其他目录。
- 不打包开发电脑的业务库、已导入文件、草稿或运行历史。私人初始化配置仅显式放入私人包，空库才初始化；不得上传私人安装包到 GitHub。
- 不启用 GitHub Actions 或其他云端 CI。

## 本地验证范围

```text
node tools/build.mjs --test --offline
node tests/install-smoke.mjs
```

本次发布前重新执行上述命令，均通过，输出 BUILD_OK / INSTALL_SMOKE_OK。完整回归包括：五角色权限、分级审批、通知与审计、统一三表上传、严格交叉违约日期分月、原子导入与覆盖决策、私人草稿／差异／审批、幂等与并发冲突、正式完成状态、筛选与授权导出、分页及上下滚动条的模拟接口。

反馈截止日期专项：平台测试 45 项、实际 HTTP 24 项、展示／导出 13 项。包含管理员权限、支行隔离、今天／过去日期拒绝、明天允许、北京时间零点、合法设置自然到期、旧日期读取、配置与审计事务回滚、重导保留、延期／取消、旧身份拒绝、重启与 V5→V6 数据保留。

安装器模拟覆盖新装、私人初始化配置、旧配置／数据保留、完整备份、校验失败与迁移失败保护。模拟用的是合成运行时和隔离测试目录，不连接用户本地 2874 数据库。

人员删除专项：真实 HTTP 31 项、综合 HTTP 270 项，覆盖五角色管理范围、跨支行与同级拒绝、确认页不写入、CSRF、并发版本与重复确认、会话撤销、审计及恢复账号。只用隔离合成人员，不停用本地现有用户。

## 升级注意事项

安装新版本前停止旧服务，默认升级原 `ZhihuiXinguan-PR0` 安装目录，不先卸载。迁移在暂存副本上验证；完整原目录备份保留。schema 6 不能用 rc.3 或更早程序打开。已有反馈截止日期保持不变，不会为了“最早明天”的新设限制自动删除已到期配置。

## 待现场验收

麒麟版本与 CPU 的实际断网安装、Win7 实际 IE／文档模式、旧安装升级后的业务核对、真实网络策略和生产数据量下性能。正式安全文案及新增行内数据模板仍待提供；正式推广前需确认传输保护。本包为联调测试版，不表示这些实机及安全事项已通过。
