# 智慧信管 0.3.0-rc.10 发布检查

2026-09-18，用户要求分行管理员设置黄色列是否必填，并合入 main、交付 U 盘安装包。本次从 main 的 rc.9 提交 `2c0f15f1724a63df2b7110f490baf7038045b237` 开发；实际发布 SHA 以包内 `SOURCE_COMMIT` 为准。

## 交付范围

- 应用 `0.3.0-rc.10` / schema 8，新增 V008 必填规则表，V001～V007 不改。
- 分行管理员分别配置三类清单黄色列；空白不阻止保存、提交和复核，只影响正式完成、进度、超期、筛选及导出。默认全部选填保留原口径。规则对全部支行和所有期次生效，不清空填写内容。
- 保留 rc.9 工作流及全部现有功能。规则版本冲突和审计事务保护，旧库升级不删除业务记录。
- 只用本地离线依赖及已校验的 Microsoft JDK 21.0.12 Linux x86_64／aarch64 缓存；从干净提交重新构建。Windows 不执行 Linux Java。
- 新目录 `zhihui-xinguan-offline-0.3.0-rc.10-kylin` 与旧包并存，不覆盖／删除旧包；复制后逐文件校验 SHA-256。
- 私人初始化配置仅显式加入私人安装包，空库才读取；升级保留账号及原配置。包中不带开发电脑业务库、上传表、草稿或 Git 历史。私人包不得公开发布。
- 不启用 GitHub CI；不重启用户当前 2874 服务，不访问其业务数据库。

## 本地验证

发布前必须执行，任一失败不交付：

```text
node tools/build.mjs --test --offline
node tests/install-smoke.mjs
git diff --check
```

2026-09-18 最终 rc.10 全量离线回归通过：BUILD_OK，CompletionRules 101、CompletionRuleHttp 75（真实 Main 五角色、三模板部分填报提交／复核／补齐／清空、正式 XLSX）、CompletionRuleView 31（含必填缺项进度 XLSX 与首页计数）；既有 WorkflowPlatform 241、Feedback007Regression 156、WorkflowRoutes 100、BusinessHttp 62、BusinessView 221 等全部套件通过。同轮 INSTALL_SMOKE_OK、git diff --check 通过。随后从同一已提交源码重新构建交付包，U 盘交付须由打包程序逐文件校验成功。

安装模拟已通过新装、私人初始化配置、升级保留数据和配置、完整备份、迁移失败保护、校验失败。真实合成 V7→V8 测试验证账号、正式值、待复核、审计、截止日期和默认规则保留，规则重启持久化，迁移失败拒绝继续启动。

使用隔离虚构库和 Chromium 验证设置页布局、原生下拉、保存回显、明细必填标记和首页进度从 3/3 到 2/3 联动。按 computer-use 技能仅通过浏览器界面操作；不连接用户业务库。安装模拟和现代浏览器不代表真实麒麟／旧 IE 实机通过。

## 升级与回退

先停止工作电脑旧服务，不卸载，再运行新包 install.sh；安装目标必须为原安装目录（默认沿用 `ZhihuiXinguan-PR0`）。迁移在暂存副本验证，成功后替换并保留完整旧版备份，保留原账号、正式数据、草稿、待办、审计及截止日期。首次升级全部选填，原完成状态不变；设置必填后历史完成状态会重算。

schema 8 不能用 rc.9 或更早二进制直接启动。回退须使用升级前完整备份并先保全升级后的新数据；备份须包含整个 data 以及 initial-password.key 和副本，不只复制数据库文件。

## 现场验收边界

真实麒麟版本／CPU、Win7 实际 IE／文档模式、实际数据量性能、离线升级后的业务核对、正式传输保护仍需现场验收。本轮不声称这些已通过，保留原 ES3／原生表单／本地静态资源兼容路线。

现场重点：三类黄色列配置 → 操作员留一项空白仍可提交 → 复核批准仍未完成 → 补齐后变绿 → 清空后重新未完成；取消全部必填恢复原口径；核对首页、支行进度、超期、筛选与两个类型导出一致。

正式安全文案及其他行内模板仍待提供，本包是联调测试版。
