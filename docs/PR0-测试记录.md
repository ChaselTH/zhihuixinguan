# PR0.2 测试记录

日期：2026-09-11；版本：0.3.0-pr0.2；源码分支：codex/v0.3-foundation。

## 已执行

- Windows / JDK 17.0.19 / Node 24.19，本地完全离线依赖构建通过。
- FoundationTest：186 项断言通过。覆盖三模板读写、用户 .et 表头匹配与示例跳过、黄色列任一非空、固定九支行、五角色权限策略、跨支行阻断、季度区间、稳定编号、幂等、版本冲突及整批回滚、旧文件内容不变、历史附加字段保留、未知旧机构隔离。
- HttpSmokeTest：36 项断言通过。真正启动隔离 localhost HTTP 服务，使用随机测试密码和虚构数据。覆盖登录门禁、三入口和模板下载、错误入口／CSRF、上传预览不发布、确认发布、单格完成、HTML 转义、重复保存幂等、首页未完成过滤、导出、重复保留与覆盖确认、导入和保存并发冲突、三模块导入、审计状态及退出失效。
- 安装器模拟通过：新装、旧数据复制、完整备份、模拟迁移失败保持原目录、归档哈希错误停止安装。使用专门的合成 Java shell 替身，不是 Linux Java 实机运行证明。
- install.sh 与 start.sh 的 Bash 语法检查通过。
- 浏览器检查：本地三上传卡片、九支行、统计范围及版本展示；发现导航对比度问题后在 PR0.2 修正。

## 未执行／不属于本阶段

未在真实麒麟 CPU／系统和 Windows 7 IE 上执行；安装包提供 x86_64 与 aarch64 JDK，需要用户实机反馈。GitHub CI 配置已加入，但未推送、未宣称云端流水线通过。真实多用户、申请审批、通知及草稿复核的端到端测试，必须等后续实现再执行。

本轮所有自动业务测试均为虚构数据；安装包白名单不包含 data、原用户表格、实际客户示例、测试数据库或临时上传文件。
