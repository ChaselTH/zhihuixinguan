# A1 / B1 集成记录

日期：2026-09-15。用户明确授权处理两个 PR、解决冲突并合并。

- A1：PR #4，原提交 `19e7155ea21676377e9fbcf69808365d78c008cc`。
- B1：PR #5，原提交 `8ad7b1f0e5993567580fa2c01d5df95db3d6204e`。
- 集成版本：`0.3.0-a1b1.1`，schema 3；没有新的数据库迁移或依赖升级。
- 先在 `codex/v0.3-a1-b1-integration` 合入双方原始提交、复核并测试，再按 #4 → #5 合入 main。双方 Git 历史保留，不使用 ours/theirs 丢弃一侧内容、不强推。

## 合并处理与接入

两条原 PR 分别可自动合并；共同的 memory.md 追加位于不同位置，自动合并后已确认两份开发记录均保留。实际要处理的是模块接入与行为不一致，不能只点 Merge：

1. Main 初始化 WorkflowRoutes，GET 在登录、强制改密和安全提示门禁之后分发；POST 在同样门禁、表单格式、2 MB 限额与 CSRF 校验之后分发。
2. 五角色公共导航加入工作流；超级管理员只有查询权限，操作员保存私人草稿并提交复核，分行管理员 / 支行管理员 / 复核员本人修改先预览再确认。
3. workflow.css 纳入本地资源白名单。通知详情通过已授权的本人通知进入 `/workflow/submission?id=...`，目标路由再次检查支行权限；已读不等于批准。
4. B 原审计链接使用 submissionId，而 A1 原筛选使用 search。新增 `AccessPlatform.auditForSubmission`，先验证单据权限，再按关联表精确查询；筛选表单与翻页保留 submissionId，不忽略参数后返回所有日志。
5. 旧 `/update-batch` 不再直接调用 publishDirect；作为兼容入口只生成 previewDirect，用户确认后才能生效，防止旧表单绕过 B1 的确认流程。保留原数据页面，未开展 B2 重设计。
6. WorkflowRoutesTest 正式加入统一构建回归；新增 WorkflowIntegrationHttpTest 在真正 Main 服务上联调，而不是仅用捕获的 exchange。

## 审查修正

- 工作台草稿原只取前 10 份且没有完整列表，补充 `/workflow/drafts` 按表种和每页 30 份查询。工作台指标明确“最近”，不把截断数量写成总量；管理员和同支行其他操作员不能看私人正文。
- 复核筛选的单行 if 缺少花括号，导致无状态选择器时仍输出 option 和关闭标签；已修正并回归。
- 支行编辑页对伪造 organization 参数改为明确拒绝，不静默改查本支行。
- 退回修订后切换筛选条件会丢失 prior 关联；筛选表单现保留旧单编号，新提交保留历史关联。
- 安装提示与迁移方式未更改；大小错误提示区分普通表单 2 MB / 上传批次 50 MB。

## 验证范围

`node tools/build.mjs --test --offline` 包含 A1 原套件、B1 工作流路由回归及新增真实 HTTP 集成。最终精确提交与断言数写入 PR #5 合并记录；不把旧独立 PR 结果当作集成证据。

真实 HTTP 覆盖：匿名与每次登录安全门、五角色导航、跨支行 ID / 查询篡改、私人草稿隔离、CSRF 与超大表单、保存 / 服务端差异 / 不可变确认、通知跳转 / 已读、原因退回 / 关联重提 / 批准 / 幂等重试、三种直接角色、精确单据审计、三表正式页面与真实 XLSX 导出。待审和草稿不进入正式数据；交叉违约同样按支行隔离。

另执行 `node tests/install-smoke.mjs`，验证安装器控制流程、新装、保留配置与数据升级、失败保留；这是虚构运行时模拟，不是麒麟实机结果。未修改或使用用户的真实业务库、初始化凭据或 U 盘安装包。

## 下一阶段边界

本次只集成 A1/B1，未放行 A2/B2。自动协作者读取 main 上的 COMPLETE 状态后应停止重复开发、等待下一次明确任务，不以“PR 已合并”推断可启动下一阶段。正式安全提示正文、正式传输保护、最低 IE / 麒麟实机兼容及最终离线安装包仍待后续验收。
