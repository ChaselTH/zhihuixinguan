# PR0.7 公共基础合并测试记录

日期：2026-09-14。版本：0.3.0-pr0.7。性质：交付用户进行麒麟／Win7 IE 实机测试，不是正式生产发布。

## 本次合并内容

- B 已有贡献：B1 就绪清单、Windows 安装器模拟的 Bash 路径发现修复、脱敏开发问题记录；随基础 PR #1 合入。
- A 公共补丁：本人草稿、草稿版本、服务端预览、冻结提交快照、批准／退回／直接确认事务、通知与个人已读、审计关联、并发／幂等／回滚和 V002 顺序迁移。
- 继续保持五角色九支行、三表导入与导出、机构隔离及原有网页。新的工作流页面尚未接入，A1/B1 按用户要求暂缓，待用户测试反馈后再安排。
- 不重置实际用户密码，不修改已有运行数据，不上传本地初始化配置或私人安装包。

## 本地验证证据

基础 PR #1 的精确源提交 `8c72d16239d9bc3bc8d4b8e1c27706c89d84bfed` 已在隔离工作目录重新执行：

```text
node tools/build.mjs --offline --cache <已校验依赖目录> --test
node tests/install-smoke.mjs
```

基础 401 项检查及安装器模拟通过。`--offline` 只改为使用已缓存并核对哈希的依赖，不省略测试。

公共补丁验证命令：

```text
node tools/build.mjs --offline --test
node tests/install-smoke.mjs
```

| 检查组 | 通过断言数 |
| --- | ---: |
| 机构联动脚本 | 12 |
| FoundationTest | 183 |
| HttpSmokeTest | 136 |
| IdentityTest | 53 |
| BootstrapTest | 18 |
| WorkflowPlatformTest | 203 |
| WorkflowReadModelTest | 210 |
| 合计 | 815 |

包括真实持久化虚构账号、同／跨支行访问、事务故障注入、并发审批与直接提交、通知个人回执、重启、V001 旧账号／记录版本／正式填写／审计保留，以及实际 XLSX 导出和首页完成数不混入草稿、待审或退回内容。

安装器模拟另行覆盖新装、保留本地初始化配置、升级后保留旧数据与备份、迁移失败及包校验失败保护。V001 SQL 未修改；脚本语法与差异空白检查通过。测试使用临时虚构数据库，未将用户工作数据库作为测试夹具。

## 交付及尚待验证

安装包自带 x86_64 与 aarch64 离线 Java。包内 SOURCE_COMMIT 记录源码提交，SHA256SUMS 校验 payload；U 盘复制逐文件校验。若显式附带 bootstrap.local.properties，整个包是私人包，不得上传 GitHub 或公开 Release。

上述记录是本地自动测试，不是 GitHub CI 或麒麟验收结论。GitHub 账单限制不通过删除 CI、改分支保护或管理员强制绕过处理；最终合并使用正常 PR 流程。真实麒麟架构／系统、离线安装与升级、Win7 实际 IE、旧 IE 访问及单位要求的传输保护仍需实机确认。

本轮建议重点测试：安装升级不丢数据、已有账号可登录、角色及机构隔离、三表导入／模板／导出、月份切换与正式完成进度、Ctrl+C 关闭和再次启动。尚无草稿／审批页面是本轮边界，不是部署失败。
