# FEEDBACK-001 修订 2：修复 PR #10 审查问题

日期：2026-09-17；状态：READY；负责人：另一台电脑的 AI（原 AI B）。

用户已明确授权把审查问题推送到 GitHub 交由你修复。继续 `codex/v0.3-feedback-001` 和 [PR #10](https://github.com/ChaselTH/zhihuixinguan/pull/10)，不另建 PR、不重做整个功能、不自行合并或发布。审查基线为 `ebc1aead6d6d418124c5bbcd4ca1386a785c5084`；原审查：[REQUEST_CHANGES](https://github.com/ChaselTH/zhihuixinguan/pull/10#pullrequestreview-5229570461)。行号针对该基线，修复后以实际位置为准。

## 已有验证证据

维护人在独立 worktree 对该 SHA 运行全量离线测试及安装模拟，均通过；这不代表新需求验收通过。没有连接用户运行中的 2874 实例，没有使用真实业务数据。补充合成数据探针实际复现：

1. 只有超级管理员的空库：四参数 `apply(..., OPERATOR)` 正确阻止无支行管理员申请，但三参数 `apply(...)` 成功写入 requestedRole=null 的待办。
2. 同认证号先申请分行，再申请其他支行，第二次仍正常返回，不提示重复。
3. 已有同认证号分行待审批申请时，管理员仍可手工创建该号码的支行操作员，原待办残留。
4. 最后一名武进支行管理员有客户经理待审申请，停用会拒绝，但将机构改为金坛且 active=true 成功，武进变为无管理员。
5. 新增角色和临时凭据字段后 schemaVersion() 仍为 4；旧 main 程序对该合成新库执行 --migrate-only 仍成功，结构升级/降级保护未生效。

## R1～R9：逐项修复及新增验收

### R1（P1）申请接口可绕过角色和上级检查

位置：`src/Main.java:37`、`src/identity/AccessPages.java:9`。

- 删除客户端可指定的 `__legacy__` 路径；同时省略 requestedRole/roleRequired 也不能退回不校验角色的旧 apply。
- 新 HTTP 申请必须由服务端强制合法目标角色和对应有效上级，不能靠隐藏字段控制是否校验。历史数据库申请兼容与新请求分开。
- 修正旧 AccessHttpTest，模拟浏览器实际 select 值，不要为了旧测试而增加绕过入口。
- 验收：三个支行角色、分行角色、空/非法/缺失角色、伪造哨兵、无上级、错误机构组合；拒绝时没有任何待办/通知写入。

### R2（P2）重复认证号仍不提示

位置：`src/platform/AccessPlatform.java:41`。

- 命中已有账号或待审批占位不能静默 return null 后显示申请已接收；返回明确的“已有账号或待审批申请”提示，不暴露其他机构详情。
- 验收：同机构/跨机构/跨角色重复、已有有效/停用账号、两个同时申请、前导零保留；唯一性和错误提示均验证。

### R3（P2）手工开户忽略待申请占位

位置：`src/platform/PlatformStore.java:61`。

- 手工 createUser 检查同认证号待审批占位。审批内部开户只允许原子消费本次正在审批的申请，不能笼统放行其他待办。
- 验收：先申请后手工新增、手工开户与审批并发，不能同时留下有效新身份和冲突待办；正常审批仍可成功，重复审批幂等。

### R4（P1）新增 DDL 没有版本化迁移

位置：`src/platform/PlatformStore.java:126-129`、`src/platform/SchemaMigrations.java`。

- 移除初始化尾部的临时 ALTER/CREATE INDEX，追加 V005，保持已发布 V001～V004 不变。
- 新结构纳入 checksum、迁移 STARTED/DONE、失败保全及版本诊断；应用 VERSION 不在本任务擅自发版，但数据库 schema 必须真实递增。
- 验收：全新库、真实合成 V4→V5 保留账号/业务/待办、逐步中断、下次启动拒绝半迁移、旧程序拒绝 V5。兼容已试跑旧 PR 而附加过字段的合成 schema-4 库，不丢数据。

### R5（P2）人员角色/机构变更绕过待办保护

位置：`src/platform/PlatformStore.java:66`。

- 停用、调机构、改角色均按变更后的实际责任检查，不能只检查 active=false。
- 核对最后一名分行管理员、支行管理员、复核员及旧申请：存在未处理责任且没有合法替代人时阻止变更并给出提示；无待办或已有替代人时允许。
- 验收包含前述武进→金坛复现，防止处理人离岗后任务无人处理；正常权限层级不扩大。

### R6（P2）明确多期次数据被整批拒绝

位置：`src/importing/ImportRoutes.java:26`。

- 当前任一类型的 periods.size()>1 就拒绝，导致两个明确不同月份/半月期次的完整工作簿无法批量导入，按提示补齐期次仍然失败。
- 允许已明确行级/文件级归属的多期次；仅对无法归属或公共月份造成实际歧义的情况提示补充，禁止静默覆盖正确归属。
- 验收：不同月份、同月不同半月、文件名推断、补充期次、交叉违约没有期次列；三种数据对应关系正确。

### R7（P2）展开明细分页丢失状态

位置：`src/importing/ImportJobPages.java:17,33` 和 choices 保存重定向。

- 分页和保存后必须保留 details=yes 与正确 offset；当前下一页回到汇总，再展开回第 1 页。
- 验收：54 条以上三表批次，第二/末页可查看、选择、保存及返回，所有页决定均保留。

### R8（P2）汇总缺少统计及直接批量决策

位置：`src/importing/ImportJobPages.java:37`。

- 从完整暂存批次聚合文件数、期次、各表条数、新增/与正式重复/填报冲突数；不能只汇总当前 25 条，不能把本批重复折叠数冒充与正式重复数。
- 汇总页直接提供保留/覆盖/跳过以及清空确认，不强迫打开逐企业长明细才能批量处理。确认后也按类型汇总。
- 验收：超过一页、部分重复、空白覆盖、各表数量不同、混合已填写/未填写；汇总决策与最终写入一致。

### R9（P2）自动已读交互及气泡更新不完整

位置：`src/Main.java:45,57`、`src/notifications/NotificationPages.java`。

- GET 中新增 markRead 绕开原 POST 的 CSRF；未读计数又在标记前读取，详情页仍显示旧气泡，且手动“标记已读”按钮仍在。
- 使用兼容旧 IE、带 CSRF 的正常 POST 中转后进入授权目标，更新未读计数，不再要求额外手工标记。保持本人隔离、目标授权、幂等、无开放重定向。
- 验收：点一条仅减少本人一条、重复点不重复计数、无 CSRF/跨用户拒绝、目标已处理或删除时提示正常。

## S1～S4：同步收尾（不能漏项）

- S1：初始密码生成/重设页面中“仅本次显示，离开只能重置”文字与再次查看功能矛盾，改为本人首次改密前可由授权管理员查看；本人改密后不可恢复。
- S2：统一上传页仍显示三个无效旧 GET 链接，移除。只保留统一入口；兼容后端接口可保留鉴权，不在 UI 冒充可用页面。
- S3：有历史密文但密钥丢失时，不静默生成新密钥混用旧/新密文。明确密钥恢复/凭据作废与重置策略，保留备份一致性、文件权限、首次改密/停用清除、查看授权、无明文日志。补密钥丢失与备份恢复测试，不擅自接触真实凭据。
- S4：报告准确区分旧测试和本次新增验收。每个 R/S 编号写明实现提交、测试用例/命令/结果，不能只写“全量通过”或用旧隐藏字段测试冒充新 UI 语义。

## 提交方式及完成条件

1. 同步任务文档，记录“修订 2 已领取”，保留原 PR 功能，不重建分支。
2. 先修 R1～R5 和 S3，再修统一导入、通知、文案；按逻辑提交，不放宽权限或跳过测试来获得绿色结果。
3. 在 `docs/ai-collaboration/reports/FEEDBACK-001.md` 追加修订 2 表格，逐项对应 R1～R9/S1～S4，写基线/最终完整 SHA、迁移及备份影响、真实结果和未实测限制。
4. 必跑 `node tools/build.mjs --test --offline`、`node tests/install-smoke.mjs`；仅本地验证，不启用/等待 GitHub CI。测试用合成数据，不访问用户运行实例。麒麟、真实 Win7 IE 未验证就注明未实测。
5. 推送原分支、更新 PR #10，回应原审查并通知维护人可复审；不得自行合并、改保护、强推 main、打包或发布。只有维护人复审后才解除阻塞。
