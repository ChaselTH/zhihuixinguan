# FEEDBACK-001 交付报告

## 认领与基线

- 负责人：另一台电脑的 AI（原 AI B）。
- 开发分支：`codex/v0.3-feedback-001`。
- 基线完整 SHA：`649e41e67ccd74436d2dc0e3b1d968d951195866`（`origin/main`，FEEDBACK-001 修订 1 / READY）。
- 功能实现提交完整 SHA：`07f27c6e35d6031f8bb63a4c2207d6be379821dd`。
- 本轮未启用 GitHub Actions，未改版本号、依赖或发布包。

## 实现清单

- 权限申请页面增加分行/支行选中态；支行申请明确选择支行管理员、操作员或复核员，分行申请固定为分行管理员。服务端持久化目标角色、校验机构/角色匹配，并检查有效上级管理员；支行管理员申请路由至分行。
- 保留认证号唯一约束、待办去重和并发事务；批准时重新核验申请目标角色及审批权限。
- 人员管理支持授权管理员再次查看未修改的系统临时初始/重置密码。密码用本地 AES-GCM 密钥加密保存，密钥位于数据目录 `platform/initial-password.key`，不提交 Git；首次改密或停用后清除可恢复副本。查看操作不写入密码值，页面响应禁止缓存。
- 删除沿用逻辑停用并使会话版本失效；最后一名支行管理员/复核员在存在对应待办时会被阻止，提示先安排替代人员。
- 点击通知详情先将本人该条通知标记已读，仍保留通知目标授权和个人隔离。
- 增加 `/imports/upload` 统一入口，支持一个或多个完整三表工作簿；缺表、重复表头、格式或字段错误会让整批拒绝。统一模板包含三张原格式数据表和说明页。
- 三表工作簿使用一个持久化暂存任务和一个确认事务，跨数据类型的正式写入、导入批次和审计一次提交，任一失败整体回滚；默认预览为汇总，可按需展开逐条明细。
- 旧单表上传路由继续保留授权兼容，不形成匿名或越权旁路。

## 数据与安全边界

- 已发布 V001～V004 未改写；schema-4 运行库通过兼容 DDL 增加反馈字段，旧库数据保持不变。
- 真实业务表、初始化配置、临时密钥和离线依赖均未加入 Git。
- 预览仍绑定创建者、身份版本、过期时间和正式数据基线；确认沿用保留/覆盖/跳过、冲突检测、幂等和审计语义。

## 本地验证

- `node tools/build.mjs --test --offline`：通过。包含统一模板解析、三表统一暂存/确认、原子回滚、角色和人员回归、通知自动已读等测试；最后输出 `BUILD_OK`。
- `node tests/install-smoke.mjs`：通过。覆盖全新安装、私有初始化、升级保留配置、迁移失败保全和校验失败；仅为合成运行环境。
- 未声称 GitHub CI、麒麟实机或 Win7 IE 验收通过；本轮未连接真实业务库。

## 尚待审查

- 已推送功能分支并创建 GitHub PR：[PR #10](https://github.com/ChaselTH/zhihuixinguan/pull/10)，等待 ChaselTH 审查，不自动合并。
- 初始密码密钥需要随数据目录备份/恢复；密钥遗失时只能重置临时密码，不能静默显示错误凭据。现代浏览器兼容检查由本地测试覆盖，旧 IE 与麒麟仍需负责人实机验收。

## 修订 2（R1～R9、S1～S4）

- 修复提交：`cb9a6f2`（`fix(feedback): address revision 2 review findings`）。本节替代上文修订 1 中关于 schema-4 尾部 DDL、通知 GET 自动已读和旧上传链接的现状描述。
- R1/R2/R3：申请接口不再接受无角色或 `__legacy__` 旁路；页面提交真实目标角色并校验上级。重复统一认证号统一返回“已有账号或待审批申请”的通用提示，不泄露机构；手工开户检测待审批占位，审批开户只原子消费当前申请编号。
- R4：新增 `resources/db/V005__feedback_roles_credentials.sql`，`SchemaMigrations.CURRENT_VERSION=5`；V001～V004 原脚本保持不变，初始化尾部不再追加隐式 DDL。迁移版本、校验和失败保全仍由版本表保护，schema-4 数据库升级后保留原数据并获得反馈字段；未来版本仍拒绝启动。
- R5：停用、调机构和改角色统一经过责任保护；最后一名分行管理员、支行管理员或复核员仍有对应待办时会阻止变更，并覆盖武进→金坛的跨机构越权回归。
- R6/R7：统一工作簿允许明确的行级/文件级混合期次，仅在期次无法消歧时拒绝；明细模式在分页、保存、确认之间保留 `details=yes` 和 offset。
- R8：统一工作簿默认汇总完整批次文件数、期次、各数据表条数、新增、正式重复、填报冲突和候选数；页面提供整批保留/覆盖/跳过选择，并要求覆盖/候选确认。
- R9：通知详情 GET 只读；列表通过带 CSRF 的 POST bridge 标记已读、更新未读数并跳转到经过授权的申请/工作流目标，详情页移除手工“标记已读”按钮。
- S1/S2/S3：初始凭据说明改为“首次页面显示、首次改密或停用后不可恢复”；人员页不再呈现旧单表上传链接。初始密码密钥在数据目录同时维护主密钥和 `.bak` 备份，主密钥缺失可用同备份恢复；两者同时缺失且数据库存在密文时拒绝启动并提示人工恢复/受控重置，不静默生成新密钥。`IdentityTest` 覆盖丢失与同密钥恢复。
- S4：本报告逐项列出实现、证据和限制；没有把云端 CI、麒麟实机或 Win7/旧 IE 运行写成已通过。

## 修订 2 本地证据

- `node tools/build.mjs --test --offline`：通过，输出 `BUILD_OK`；包含身份、访问申请/审批、责任保护、通知 POST bridge、统一导入汇总/期次/分页和平台回归（合成数据）。
- `node tests/install-smoke.mjs`：通过，覆盖全新安装、私有初始化、升级保留本地配置和密钥备份、迁移失败保全、校验失败；为合成运行环境，不等同于麒麟实机验收。
- `git diff --check`：通过。未新增或恢复 GitHub Actions，未修改版本号、发布包或真实业务数据。

## 当前状态

- 修订 2 已提交并推送原分支 `codex/v0.3-feedback-001`，远端 head 已更新至 `446587d7e6f2f88cbb1b2a2700df80b6a70cfe6d`，继续使用 PR #10；PR 保持 OPEN，等待 ChaselTH 重新审查，未自行合并。

## 修订 2 复审补丁（R1、R6）

- 追加提交：`0a8de65`（`fix(feedback): close null-role and cross-month gaps`）。
- R1 补强：四参数平台申请入口也强制要求非空目标角色；无管理员和已有管理员两种场景均在事务外立即拒绝，申请、认证号占位和通知均不新增。历史无角色记录仍只作为历史数据读取/审批对象，不作为新申请兼容入口。
- R6 补强：行内明确期次优先；缺失行期次时按补充期次、文件名期次／月份解析；交叉违约表在公共月份与文件月份冲突时返回错误并整批不暂存，避免跨月同客户记录被错误去重。公共月份与补充期次冲突也显式拒绝。
- 新增证据：`AccessPlatformTest` 78 项断言；`ImportWorkbookTest` 219 项断言（文件月份推断、公共月份冲突）；`ImportHttpTest` 55 项断言（两个完整跨月工作簿真实 HTTP 上传→确认→按月查询／导出，冲突上传零写入）。以上均为合成数据。
