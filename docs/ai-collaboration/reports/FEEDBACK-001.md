# FEEDBACK-001 交付报告

## 认领与基线

- 负责人：另一台电脑的 AI（原 AI B）。
- 开发分支：`codex/v0.3-feedback-001`。
- 基线完整 SHA：`649e41e67ccd74436d2dc0e3b1d968d951195866`（`origin/main`，FEEDBACK-001 修订 1 / READY）。
- 功能实现提交完整 SHA：`07f27c6e35d6031f8bb63a4c2207d6be379821dd`。
- 本轮未启用 GitHub Actions，未改版本号、依赖或发布包。

## 实现清单

- 权限申请页面增加分行/支行选中态；支行申请明确选择支行管理员、操作员或复核员，分行申请固定为分行管理员。服务端持久化目标角色、校验机构/角色匹配，并检查有效上级管理员；支行管理员申请路由至分行。
- 保留认证号唯一约束、待办去重和并发事务；批准时重新核验申请目标角色及审批权限。
- 人员管理支持授权管理员再次查看未修改的系统临时初始/重置密码。密码用本地 AES-GCM 密钥加密保存，密钥位于数据目录 `platform/initial-password.key`，不提交 Git；首次改密或停用后清除可恢复副本。查看操作不写入密码值，页面响应禁止缓存。
- 删除沿用逻辑停用并使会话版本失效；最后一名支行管理员/复核员在存在对应待办时会被阻止，提示先安排替代人员。
- 点击通知详情先将本人该条通知标记已读，仍保留通知目标授权和个人隔离。
- 增加 `/imports/upload` 统一入口，支持一个或多个完整三表工作簿；缺表、重复表头、格式或字段错误会让整批拒绝。统一模板包含三张原格式数据表和说明页。
- 三表工作簿使用一个持久化暂存任务和一个确认事务，跨数据类型的正式写入、导入批次和审计一次提交，任一失败整体回滚；默认预览为汇总，可按需展开逐条明细。
- 旧单表上传路由继续保留授权兼容，不形成匿名或越权旁路。

## 数据与安全边界

- 已发布 V001～V004 未改写；schema-4 运行库通过兼容 DDL 增加反馈字段，旧库数据保持不变。
- 真实业务表、初始化配置、临时密钥和离线依赖均未加入 Git。
- 预览仍绑定创建者、身份版本、过期时间和正式数据基线；确认沿用保留/覆盖/跳过、冲突检测、幂等和审计语义。

## 本地验证

- `node tools/build.mjs --test --offline`：通过。包含统一模板解析、三表统一暂存/确认、原子回滚、角色和人员回归、通知自动已读等测试；最后输出 `BUILD_OK`。
- `node tests/install-smoke.mjs`：通过。覆盖全新安装、私有初始化、升级保留配置、迁移失败保全和校验失败；仅为合成运行环境。
- 未声称 GitHub CI、麒麟实机或 Win7 IE 验收通过；本轮未连接真实业务库。

## 尚待审查

- 已推送功能分支并创建 GitHub PR：[PR #10](https://github.com/ChaselTH/zhihuixinguan/pull/10)，等待 ChaselTH 审查，不自动合并。
- 初始密码密钥需要随数据目录备份/恢复；密钥遗失时只能重置临时密码，不能静默显示错误凭据。现代浏览器兼容检查由本地测试覆盖，旧 IE 与麒麟仍需负责人实机验收。
