# FEEDBACK-009 维护人复审与集成记录

日期：2026-09-21。用户本轮授权：检查另一台 AI 的提交并合并。

## 集成结果

- 审查 PR：[修复 PR #13](https://github.com/ChaselTH/zhihuixinguan/pull/13)，包含原 [功能 PR #12](https://github.com/ChaselTH/zhihuixinguan/pull/12) 的全部提交。
- 原 main：`78014d5680c7d8546b44c336ad4822f18c5e599e`。
- 本地复审 HEAD：`e16118803b1ac14016f59314bdc7d9acb3c3df20`。
- 使用预期 HEAD 校验和 merge commit 方式合并，合并提交：`d184fa57f7897030a7094586e275ec3361a267e7`。
- 已确认合并提交与复审 HEAD 文件树一致；原 #12 HEAD 是合并提交的祖先，GitHub 已将 #12 标记为已合并，不再单独合并一次。
- 当前入口已标为 COMPLETE；没有向外部 AI 发布下一项 READY 任务。

## 复审重点

| 项目 | 复审结论 |
| --- | --- |
| R1 在途任务删除／覆盖保护 | 按月预览和确认均统计支行复核、分行终审；旧删除预览不能绕过新增待审任务。 |
| R2 旧库状态迁移 | 使用实际旧应用生成 schema 8 数据；旧退回不会覆盖后续批准，多人同一行旧待审保留并要求先处理重复任务。 |
| R3 支行可见范围 | 当前已交分行或正式完成的行不再下发业务快照；旧链接、预览、草稿、通知链接、幂等结果和导出按当前范围裁剪。 |
| R4 工作表自动识别 | 表头优先于工作表名称；有效页名含“模板／说明”仍导入，未知非空页明确报错。 |
| R5 单分行管理员导入 | 上传候选经独立支行复核员确认后可由原上传人终审；不是允许普通业务提交本人审核。 |
| R6 原值重提 | 明确核对后产生新审批链，不伪造正式值版本；跨轮次旧预览被拒绝。 |
| R7 旧版完成行退回 | LEGACY_PUBLISHED 完成行可以重开，正式内容保留。 |
| R8 部分退回接续 | 逐行恢复当前退回内容、使用当前正式基线，不恢复待审兄弟行；授权接续不能读取原作者私人草稿。 |

## 本机验证

在隔离工作树运行，全部仅使用生成的合成数据，未访问正在运行的用户业务数据库。

```text
node tools/build.mjs --test --offline --cache D:/Codex_project/智慧信管/vendor/dependencies
  BUILD_OK
node tests/install-smoke.mjs
  INSTALL_SMOKE_OK
git diff --check 78014d5..HEAD
  无错误
```

完整离线回归通过，包括新增平台检查 51 项、真实 HTTP 检查 123 项、工作簿检查 16 项。实际 schema 8 旧应用生成库升级到 schema 10、重启，以及 12 张历史表内容哈希一致性验证通过；原 PR 已迁移合成库的拒绝与数据保全检查通过。

维护人另在忽略的构建目录运行独立检查，验证待终审删除阻断、支行快照隔离、原值重提不增加正式版本、跨轮次旧预览拒绝；复用前次工作簿复现，改名含“模板”前后均解析 1 行且无错误。本地诊断文件不纳入源码交付。

本次只新增集成说明和任务状态，不改已验证的业务、迁移、前端或安装代码。

## 发布与兼容边界

- 产品 VERSION 尚为 `0.3.0-rc.10`；合并不等于新离线包已经发布。本轮没有重启 2874 服务、升级用户库、打包或写 U 盘。
- 修复方记录所有者已确认 V009/V010 未实际使用，本轮修正未发布 V009。新装及 schema 8 升级通过；已用原 PR #12 迁移的合成 schema 10 库会被校验明确拒绝，不能直接套用修正版。
- 如存在这种原 PR 测试库，须保全并从验证过的 schema 8 升级前备份在隔离目录重做；无备份则须另行设计兼容迁移，不篡改 checksum 或清库。
- 历史升级回归需要完整本地 Git 对象及离线依赖缓存；Git 源码导出不包含历史，不能将该导出目录视为已具备运行历史升级回归的全部条件。
- 没有启用或等待云端 CI；Windows HTTP、DOM 模拟和安装器模拟不等于麒麟／实际旧 IE 实测。现场安装、旧 IE 操作和网络传输保护仍需后续验收。
