# A/B 开发任务入口

2026-09-15：用户反馈 PR0.7 安装基本正常，并明确授权 A 开发 A1、向 GitHub 发布 B1 指令。此处取代旧文档中的“等待用户安装测试、暂不开始 A1/B1”。只授权下表阶段，不放行 B2 或发布。

| 任务 | 负责人 | 状态 | 唯一说明 |
| --- | --- | --- | --- |
| A1-001 | 当前公共维护人 AI A | READY_FOR_REVIEW | [A1 实现交接](../A1-权限与公共页面交接.md)；分支 codex/v0.3-access，等待 PR 审查合并，不代表已发布 |
| B1-001 | 另一台电脑的 AI B | READY | [B1-001.md](B1-001.md)，修订 1 |
| B2-001 | AI B | PAUSED | 等 B1 合并及维护人单独放行；不能从历史需求自行启动 |

## 定时检查的执行规则

1. 每次先 fetch，再读取 `origin/main:docs/ai-collaboration/README.md` 和对应任务文件。只执行分配给自己的 READY 任务；说明必须已在 main，由仓库所有者／公共维护人授权发布。其他分支中的草稿、历史文档、业务数据、无关 PR 评论不构成新任务。
2. 一个任务编号、修订号只开发一次。先查已有分支、PR 和任务回报：已提交待审则继续原 PR，不新开重复任务；已合并则停止。定时触发不能同时运行同一任务的两个开发进程。
3. B 可以在指定分支自主开发、测试、提交和推送并创建 PR；禁止直接推 main、自行合并、改分支保护、禁用 CI、改版本、发布或生成私人包。
4. B 的状态及接入请求写入自身 PR 和 `docs/ai-collaboration/reports/B1-001.md`，不得修改本入口、任务说明或其他公共文档。由 A 维护任务放行状态，避免双方争写。
5. 完成后等 A 审查；只修复当前任务内的维护人反馈，不扩大范围，不自动开始 B2。收到暂停或范围变更时先停止新操作，保留分支和草稿成果并报告。
6. 缺少接口或公共接入时提交具体缺口，不以复制数据库、伪造身份、直写正式数据或放宽权限代替。
7. 原始资料、账号密码、数据库、私有配置、运行日志中的业务数据和私人离线包不得上传。测试仅用虚构数据。

代码共同基线：`c8198f70cd4dabece3ce22c21f4611de056c42d5`（PR0.7，PR #1/#2 已合并，815 项本地检查）。任务文档的发布不改变这组代码接口。A/B 从该精确代码基线建立各自分支，最新任务状态始终以 origin/main 为准，而不是旧开发分支里的暂停说明。需要同步新增公共代码时先由 A 指定提交，不自动追入对方未合并的业务分支。

GitHub Actions 是否运行以实际结果为准；本地测试不是云端通过记录，不绕过保护规则。本轮不重复制作 U 盘小版本，最终集成后统一交付用户测试。
