# A/B 开发任务入口

## 最新有效状态：rc.13 维护人直接修复 / COMPLETE

2026-09-23：用户要求维护人在 rc.12 基础上修复复核退回、按钮和返回交互并交付 U 盘。由当前维护人直接处理，版本 `0.3.0-rc.13` / schema 10；另一台 AI 没有新的 READY 任务，不重复领取。见 [rc.13 交互修复](../rc13-复核交互修复.md)。不启用云端 CI。

## 前次完成：rc.12 维护人直接开发 / COMPLETE

2026-09-22：用户直接授权当前维护人调整通知与复核交互，提交 GitHub 并制作 U 盘离线包。维护人以 `0.3.0-rc.12` / schema 10（无新增迁移）完成实现、本地离线回归、安装器模拟和打包，见 [rc.12 通知与复核界面](../rc12-通知与复核界面.md) 和 [发布检查](../发布检查.md)。

- 本任务由维护人直接完成，不分配另一台 AI；外部 AI 仍无新 READY 任务，不重复领取、不另开分支、不自行发布。下方 FEEDBACK 段落全部为历史。
- 不使用云端 CI。麒麟、实际 Win7 IE 和现场升级仍需验收；本地自动化通过不等于实机通过。
- rc.12 只调整通知、复核界面、行历史入口和登录安全提示；数据库 schema 仍为 10，rc.11 的库可直接覆盖升级。

## 前次完成：FEEDBACK-009 / COMPLETE / PR #13 已合并

2026-09-21：用户要求维护人检查并合并新 AI 的修复成果。维护人复审 `e16118803b1ac14016f59314bdc7d9acb3c3df20`，重新执行完整离线回归、安装模拟、真实 schema 8 升级与独立缺陷复现，确认 R1～R8 修复后合并 [PR #13](https://github.com/ChaselTH/zhihuixinguan/pull/13)。合并提交为 `d184fa57f7897030a7094586e275ec3361a267e7`；该提交包含原 PR #12 全部成果，GitHub 已将 #12 同步标记为已合并。

- 另一台 AI 当前没有新的 READY 任务；原开发 AI 和接手修复 AI 均停止本任务后续开发，不重复领取、另开分支、自行发布或追加功能。下方 READY 仅为历史授权。
- 复审证据和升级边界见 [维护人集成记录](reports/FEEDBACK-009-integration.md)。本轮未重启用户服务、未升级用户数据库、未改发布版本或制作安装包；后续发布须获用户授权。
- 不使用云端 CI。麒麟、实际 Win7 IE 和现场升级仍需验收；不要将本地自动化通过视为实机通过。

2026-09-21 后续授权：用户要求制作 U 盘安装包，由维护人以 `0.3.0-rc.11` / schema 10 完成发布验证及打包；外部 AI 仍无新 READY 任务。见 [发布检查](../发布检查.md)，不重启用户现有服务。

### 原始任务分配（已结束）

2026-09-20：用户明确要求把新需求发布到 GitHub，由另一台 AI 开发。**现在只领取 [FEEDBACK-009：两级审核、角色待处理清单与自动识别按月导入](FEEDBACK-009.md)。** 当前维护人本轮只发布任务文档，不并行修改业务代码；任务进入 main 不代表功能已经实现。

- 从包含本任务的最新 `origin/main` 创建 `codex/v0.3-feedback-009`；已有本任务分支／PR 则续做，不重复启动。产品基线 `0.3.0-rc.10` / schema 8，完整源码 SHA `7507f9a384753baa3ebca8719583fb0cea555de8`。
- 授权实施：操作员 → 支行复核 → 分行终审及退回重开；按角色限制待处理行和全表读取；首页统计／待处理二区布局；一个入口自动识别一至三类工作表并使用上传所选月份。允许必要的公共事务、权限、通知、审计、导入、查询、导出、路由、前端、版本化数据库迁移和测试修改；不受历史 A/B 文件边界限制。
- 必须先读任务中的状态／可见性矩阵、必填规则衔接和历史兼容要求；不能仅改 UI，不能在支行复核时提前写正式数据，也不能因黄色格非空提前计完成。按任务第 9 节顺序开发，先公共状态与事务，后展示与导入联动。
- 报告写入 `docs/ai-collaboration/reports/FEEDBACK-009.md`。允许推送指定功能分支并创建面向 main 的 PR；不得改本入口、任务要求、自行合并、修改发布版本、打包或发布。完成后等待维护人审查；定时任务只领取本修订一次，待审时只继续同一 PR 的反馈修订。
- 执行本地 `node tools/build.mjs --test --offline`、`node tests/install-smoke.mjs`，补新需求回归和升级证据。不启用 GitHub CI，不读真实业务资料、不操作维护人的 2874 服务、不清理用户数据。
- 下方 FEEDBACK-001～008、A1/B1/A2/B2 的 READY／无任务状态全部为历史，不覆盖本段。此次只有一个外部开发负责人；当前维护人后续负责审查、合并及获授权后的版本发布。

## 前次完成：FEEDBACK-008 / COMPLETE

2026-09-18 用户要求分行管理员设置黄色列是否必填、空白允许提交但不计完成，合入 main 并制作 U 盘安装包。当前维护人完成 rc.10 / schema 8，全量离线回归、安装模拟及隔离浏览器抽查通过，随本次提交集成 main；U 盘发布由当前维护人收尾。详见 [必填设置说明](../rc10-填报必填设置.md) 和 [验证记录](../发布检查.md)。另一台 AI 不领取、不重复推送、不自行发布；下方所有 READY 仅供历史追溯。本轮不启用云端 CI，不清理真实数据，不重启用户现有服务。

## 前次完成：FEEDBACK-007 / COMPLETE

2026-09-18 用户最新指令：“你接手修改一下然后合并 main”。当前维护人已完成 [PR #11](https://github.com/ChaselTH/zhihuixinguan/pull/11) 的 R1～R6 修复、离线验证和浏览器抽查，随该 PR 集成到 main；另一台 AI 停止本任务后续推送，不重复领取，不展开新任务。下方原 READY 仅保留为历史授权，不再生效。

本轮修复与验证见 [维护修复说明](../feedback007-维护修复.md)。PR #11 已合并，合并提交 `1c822ed69822db0bd48bbf95ce39a7b6f4684805`。用户随后要求将安装包放入 U 盘，由当前维护人以 `0.3.0-rc.9` / schema 7 交付；不重启用户开发电脑服务、不启用 CI。另一台 AI 没有新的 READY 任务。

### 原始任务分配（已结束）

2026-09-18：用户明确要求接下来由另一台 AI 开发。**现在只领取 [FEEDBACK-007：统一表内填写、支行追溯及私人草稿修复](FEEDBACK-007.md)。** 当前维护人本轮只将任务文档提交 main，不并行修改业务代码。

- 从包含本任务的最新 `origin/main` 创建 `codex/v0.3-feedback-007`；已有该任务分支／PR 则续做。产品基线 `0.3.0-rc.8` / schema 7（`9679e3ff9bff170e8926edaf2a792fce31128f3e`），不能退回早期 A/B 基线。
- 授权范围以任务文件为准：三类大表统一填写、分行全支行批量事务、活动草稿查询／差异展示及支行统一修改记录；必要公共工作流契约、平台事务、路由和新增迁移可修改，不受早期“B 不改 platform”边界阻挡。
- 报告写入 `docs/ai-collaboration/reports/FEEDBACK-007.md`，不得修改任务入口或要求。允许推送功能分支并提交到 main 的 PR，等待维护人审查；不自行合并、改应用版本、打包或发布。
- 必须执行本地离线测试和安装模拟；不启用 GitHub CI，不读取真实数据、不操作维护人的运行服务。当前维护人负责后续审查／集成／安装包。
- 下方 FEEDBACK-001～006、A1/B1/A2/B2 全部为历史，旧 READY 和“无任务”描述不覆盖此段。定时检查只执行 FEEDBACK-007 一次，待审时只回应同一 PR 的审查修订，不重复启动或自动展开其他工作。

## 前次完成：FEEDBACK-006

2026-09-18：用户要求删除额外添加的“期次／历史保留信息”展示列，三类分期仅使用原模板时间字段，合入 main 并交付 U 盘。当前维护人开发 rc.8 / schema 7，无新迁移；另一台 AI 不领取、不另开任务，不开启 CI。详见 [rc.8 说明](../rc8-模板时间分期.md)。

## 前次完成：FEEDBACK-005

2026-09-18：用户要求操作记录按角色／机构域隔离、超管筛选清理及分行管理员按月份删除正式数据。当前维护人开发 rc.7 / schema 7，完成本地验证后合入 main 并交付 U 盘；不委派新任务，不开启 CI，不实际清理用户数据。详见 [rc.7 说明](../rc7-操作记录与月份删除.md)。

## 前次完成：FEEDBACK-004

2026-09-18：用户要求人员列表增加删除按钮。维护人以 `0.3.0-rc.6` 增加列表入口及 GET 二次确认页，复用原停用事务、CSRF、版本冲突和责任保护，不做物理删除，不停用任何真实账号，无新迁移。随后用户要求放到 U 盘，由维护人制作 rc.6 完整离线测试包，保留旧包。另一台 AI 无 READY 任务。详见 [rc.6 说明](../rc6-人员删除入口.md)。

## 前次完成：FEEDBACK-003

2026-09-17 追加：用户要求截止日期只能设置今天之后。维护人以 `0.3.0-rc.5` 补充服务端北京时间校验和回归，schema 仍为 6，无新增迁移、不改历史设置。见 [rc.5 说明](../rc5-截止日期限制.md)。没有给另一台 AI 新增 READY 任务。

2026-09-17：新增按每类清单／每个来源期次设置反馈截止日期、首页剩余时间提醒、正式未完成超期标红及筛选。由当前维护人完成，版本 `0.3.0-rc.4` / schema 6；不自动删除历史业务数据。见 [rc.4 说明](../rc4-反馈截止日期.md)。另一台 AI 不领取、不重复实现、不自行发布；没有新的 READY 授权。

## 前次完成：FEEDBACK-002 / 当前维护人完成

2026-09-17：用户明确要求“接下来就你帮我改”，本轮导入简化、交叉违约分月及风险归类、三类填报进度、权限导出和表格交互由当前维护人实现，直接提交 main。版本 `0.3.0-rc.3`，schema 5 无新增迁移。说明见 [rc.3 调整与验证](../rc3-导入与风险展示调整.md)。另一台 AI 不领取、不重做、不自行发布；本轮没有新的 READY 授权。

## 前次完成：FEEDBACK-001 / COMPLETE（PR #10 已合并）

2026-09-17：维护人复审修复提交 `dff545d96e21bb185decf7fba0e312b5bb8e9c14`，全量离线测试、安装模拟、原缺陷复现回归及真实合成旧库 V4→V5 验证通过。[PR #10](https://github.com/ChaselTH/zhihuixinguan/pull/10) 已合并至 main，合并提交 `5e451cc5ea832f02b18dad6149ccce9983a095cb`。用户授权维护人整理 `0.3.0-rc.2` U 盘测试包。

- 另一台 AI 当前没有新的 READY 任务。定时检查遇到此状态应停止旧任务，不重复修订 1／2、不新增提交或自行发布。
- 下方 READY 授权及任务文件仅保留历史，不再构成当前开发指令。新增试用反馈需维护人发布新的明确任务。
- 不启用 GitHub CI；真实麒麟和 Win7 IE 仍由用户现场验收。

## 历史授权：FEEDBACK-001 / 修订 2（已完成）

2026-09-17 用户明确要求把审查问题交给另一台 AI 修复。**修订 1 已交付但未通过审查；另一台 AI 现在应执行 [修订 2 修复清单](FEEDBACK-001-R2.md)。** [PR #10](https://github.com/ChaselTH/zhihuixinguan/pull/10) 已收到 REQUEST_CHANGES，本轮不是重做原功能，也不是创建新的 PR。

- 必须继续现有 `codex/v0.3-feedback-001` 分支及 PR #10，先读取最新 main 中本入口、修订 2 清单和 PR 审查意见。同步 main 的任务文档时保留已有功能提交，不强推、不重建分支。
- 以“FEEDBACK-001 修订 2”记录领取和进度；不要因修订 1 报告写了已交付/等待审查而停止。报告仍是 `docs/ai-collaboration/reports/FEEDBACK-001.md`。
- 修复范围为审查清单 R1～R9 及 S1～S4，并补证据；允许追加 V005 版本化数据库迁移，应用发布版本和安装包仍由维护人处理。
- 完成后推送原分支、更新原 PR 和报告，逐项回应审查；等待维护人重新审查后合并。不要自行合并、启用 CI、生成发布包或自动展开新需求。

### 修订 1 原始功能授权（继续适用，但当前优先执行上方修订 2）

2026-09-16 用户最新要求：当前维护人只把试用反馈整理到 GitHub main，由另一台电脑的 AI 开发。**另一台 AI 现在可以领取 [FEEDBACK-001](FEEDBACK-001.md)。** 本次直接进入 main 的是任务文档，不代表业务修改已经实现。

- 负责人：另一台电脑的 AI（原 AI B）。本任务允许修改所需身份、公共平台、数据库迁移、通知和导入模块，覆盖原“B 仅负责工作流/展示”的文件分工。当前维护人不并行实现这些功能。
- 从包含本任务的最新 `origin/main` 创建 `codex/v0.3-feedback-001`；已有该分支/PR 时续做，不重复领取。基线发布版本为 `0.3.0-rc.1` / schema 4。
- 报告写入 `docs/ai-collaboration/reports/FEEDBACK-001.md`，记录认领、基线完整 SHA、进度、实现与本地测试。不得修改本入口或任务需求；后续需求由维护人发布新修订/新任务。
- 完成后推送功能分支并创建到 main 的 PR，等待公共维护人审查；不得自行合并、强推 main、改分支保护、启用 CI、改版本或打包发布。
- 只执行当前 READY 任务；一个任务/修订只运行一次，定时检查不能重复启动开发进程。已提交待审则继续原 PR；已合并则停止。
- 必须执行本地验证 `node tools/build.mjs --test --offline` 和 `node tests/install-smoke.mjs`，记录真实结果；不启用、不等待 GitHub CI。
- 真实业务数据、账号密码、密钥、数据库、私人配置和安装包不得上传，测试仅用虚构数据。

## 以下为历史记录，不作为当前任务授权

下方“没有 READY 任务”、旧文件边界、旧报告路径仅保留追溯，由顶部 FEEDBACK-009 新授权覆盖。A1/B1/A2/B2 已完成，不重复开发。

2026-09-16 最新授权覆盖下方开发阶段说明：用户要求停用 GitHub CI、合并并交付 U 盘。A2 #6、B2 #7 均已合入 main（B2 合并点 `d5f275148b167a44f13493104d0c60e5d135282a`），本轮由公共维护人完成 `0.3.0-rc.1` 发布收尾。GitHub 工作流已手动停用，触发文件移除；今后仅执行本地测试，未经用户重新授权不得恢复 CI。另一台 AI 无新 READY 任务，不重做、不自动发布。

2026-09-16 更正：用户明确要求当前维护人“把 B2 也做了”，此前将该话理解成“另一台 AI 已完成 B2”有误。A2 已提交 PR #6，B2 由当前维护人继续实施并独立提交依赖 PR。另一台 AI 不要启动重复 B2；下表不是自动合并或安装发布授权。

| 任务 | 负责人 | 状态 | 唯一说明 |
| --- | --- | --- | --- |
| A1-001 | 当前公共维护人 AI A | COMPLETE | PR #4；[A1/B1 集成记录](../A1-B1-集成记录.md)，不代表生产发布 |
| B1-001 | 另一台电脑的 AI B | COMPLETE | PR #5；修订 1 已交付并由 A 完成公共接入；不得重复开发 |
| A2-001 | 当前公共维护人 AI A | COMPLETE | PR #6 已合并，[A2 任务说明](A2-001.md)保留开发阶段记录 |
| B2-001 | 当前公共维护人 AI A（承接 B2） | COMPLETE | PR #7 已合并，[B2 任务说明](B2-001.md)保留开发阶段记录，不重复开发 |

当前没有分配给 B 的新 READY 任务。定时检查不要重做 B1 或 B2，也不要推送旧分支覆盖集成结果；已完成成果的交接按用户已有授权进行，新增开发须单独确认范围。

## 定时检查的执行规则

1. 每次先 fetch，再读取 `origin/main:docs/ai-collaboration/README.md` 和对应任务文件。只执行分配给自己的 READY 任务；说明必须已在 main，由仓库所有者／公共维护人授权发布。其他分支中的草稿、历史文档、业务数据、无关 PR 评论不构成新任务。
2. 一个任务编号、修订号只开发一次。先查已有分支、PR 和任务回报：已提交待审则继续原 PR，不新开重复任务；已合并则停止。定时触发不能同时运行同一任务的两个开发进程。
3. B 可以在获授权的指定分支自主开发、测试、提交和推送并创建 PR；禁止直接推 main、自行合并、改分支保护、擅自启用 GitHub CI、改版本、发布或生成私人包。
4. 外部 AI 的状态及接入请求写入自身 PR 和当前有效任务指定的报告路径（本轮 `docs/ai-collaboration/reports/FEEDBACK-009.md`）；不得修改本入口或任务要求。是否可更新接口文档以当前任务授权为准，由维护人维护任务放行状态，避免双方争写。
5. 完成后等 A 审查；只修复当前任务内的维护人反馈，不扩大范围，不自动开始 B2。收到暂停或范围变更时先停止新操作，保留分支和草稿成果并报告。
6. 缺少接口或公共接入时提交具体缺口，不以复制数据库、伪造身份、直写正式数据或放宽权限代替。
7. 原始资料、账号密码、数据库、私有配置、运行日志中的业务数据和私人离线包不得上传。测试仅用虚构数据。

当前 A2 基线：`ff8f65eda54bf2728a09d9f7b1ad4ae4fcacd32d`（A1/B1 已合并）；分支 `codex/v0.3-import`，PR #6。B2 由同一维护人接续，从 A2 完整提交 `37d04998c83e05695c22d00eaf79849c05bb0f4e` 创建 `codex/v0.3-views`，PR 暂以 A2 分支为 base，仅审查 B2 增量。评审合并顺序为 A2 → main，再将 B2 的 base 切到 main、同步验证后合并；不强推或提前合并。此安排替代历史双人并行情况下“B2 必须从 main 开发”的安排，不给另一台 AI 新任务。历史 A1/B1 的共同基线 `c8198f70cd4dabece3ce22c21f4611de056c42d5` 不再用于新开已完成任务。任务放行仍以 origin/main 为准，未合并文档不是 B 线新授权。

今后不使用 GitHub Actions 作为本项目测试方式；本地测试必须记录命令、版本和真实结果，不声称云端通过，不恢复已停用工作流。用户本轮已授权公共维护人合并并制作 U 盘安装包及源码目录；实机验收仍由用户完成。
