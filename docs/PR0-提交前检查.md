# PR0.6 提交前检查

检查日期：2026-09-14。目标分支：codex/v0.3-foundation；当前功能版本：0.3.0-pr0.6。本页记录提交前结果，不代表已经合并或正式投产。

本页保留 PR0.6 当次提交检查和推送边界。后续分支安排已修订为以 main 为共同开发基线，参见 [公共接口与分工](foundation-contracts.md) 与 [B1 工作流就绪清单](B1-工作流就绪清单.md)；下方当次“不修改 main”不代表永不合并。既有 401 项检查不覆盖尚未实现的工作流事务。

## 验证结果

- 离线构建及回归通过：机构联动 12、FoundationTest 183、HttpSmokeTest 135、IdentityTest 53、BootstrapTest 18，共 401 项。
- 安装器模拟通过：首次安装、本地初始化配置复制、升级保留旧配置和数据、完整备份、迁移失败保护、校验失败拒绝安装。
- 真实麒麟及最低 Win7 IE 版本仍须实机验收。

## 文档与共同基础

- README、安装说明及开发契约与当前版本一致，区分历史版本、已实现及待开发功能。
- 后续 AI 任务不重复账号及基础展示；无风险暂缓，截止字段未给出时不计算超时，采用实际类名和模板字段。
- B1 启动前仍须单独合并通知发布及事务式审批正式写入接口，不能把现有直接保存接口当作审批入口。
- SQL 检出换行固定 LF，V001 内容未修改，避免跨系统改变迁移校验值。
- 本地配置、H2 数据库、原业务表格、原始内部材料、构建目录及私人安装包不属于源码提交范围。

## 凭据处理及历史风险

仓库为公开仓库。经用户确认，个人初始化凭据已移到 Git 忽略的本地文件，源码、当前说明和测试不再包含真实初始化账号及密码；全部测试改用虚构配置。既有账号及密码不作重置，空库初始化才读取本地配置。

普通构建不收集本地凭据；私人离线包只有明确传入配置参数才携带初始化文件，整个私人目录和压缩包不得公开发布。

旧版 main 提交 ccbfbcf 已含旧初始密码。删除新版本常量不能清除公开历史，也不能恢复旧密码的保密性，因此仍建议更换已暴露密码。本次不改仓库可见性、不重写历史、不擅自修改实际账号。

## PR0.6 当次推送边界（历史记录）

已通过电脑既有本地代理验证 Git 连接并读取远端基线。只向 codex/v0.3-foundation 推送正常提交，不强推、不修改 main、不自动创建 PR 或派发其他 AI 任务。最终提交内容须再检查文件清单及凭据；远端提交与 CI 结果以实际推送后的 GitHub 记录为准。
