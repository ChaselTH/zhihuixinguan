# 开发问题记录

## 2026-09-18：FEEDBACK-007 R1～R6 审查路径修复

- 现象：统一大表跨页保存会丢失前页草稿；支行历史链接把显示名当机构键且二次转义；页面未加载防丢脚本；历史日期误按来源期次筛选；第 101 个草稿无法恢复；恢复最后差异后零差异草稿仍出现在活动列表。
- 原因：草稿上下文只从当前页状态推断，历史入口没有统一机构编码／参数边界，脚本只挂在旧工作流编辑页，历史从当前有效正式行反查且把日期传给来源期次查询，恢复用前 100 条列表判活，活动查询只排除提交版本而未比较快照与当前正式值。
- 解决：在业务筛选、分页和表单中携带并校验草稿 ID，保存后回到原统一大表；活动版本按精确 ID／版本、提交状态和有效字段差异判断；历史从不可变提交快照读取并按提交事件时间及独立来源期次过滤，保留撤除记录；机构参数统一转正式键、只 HTML 编码一次；RiskPages 直接加载 ES3/attachEvent 防丢脚本并覆盖 `/update-batch`；新增跨页、真实统一表、超百条草稿、零差异和历史入口回归。

## 2026-09-18：审查修复中草稿差异判断辅助方法签名误用

- 现象：首次把草稿零差异过滤抽成 `hasEffectiveChange` 时，保存草稿流对 `SnapshotRow` 调用了只接收 `Draft` 的重载，离线编译报类型不能转换。
- 原因：草稿整体是否有差异和单行是否有差异使用了同名方法，但重构后没有保留单行辅助重载，Java 泛型流的编译错误直到构建阶段才暴露。
- 解决：增加显式的 `SnapshotRow` 差异判断重载，`Draft` 判断只聚合行级结果；重新执行源码离线构建通过。

## 2026-09-18：FEEDBACK-007 分行管理员跨支行直接保存被单支行快照约束阻断

- 现象：分行管理员在统一业务表同时修改武进、金坛记录时，服务端预览直接报“一单只能包含同一家支行”，即使页面允许全行筛选也无法一次确认。
- 原因：工作流 `snapshot` 与 `workflow_previews.organization_id` 仍把所有直接修改沿用为单支行模型，未区分分行管理员的全行授权；跨支行提交若直接复用单支行页面还可能把差异归到错误机构。
- 解决：仅对分行管理员的 `DIRECT_EDIT` 放开多机构快照，预览／提交机构使用分行域；正式写入仍逐行重复执行机构权限、字段白名单、版本冲突与同一事务回滚。提交详情、提交列表、记录追溯和审计按可见行裁剪，支行看不到其他支行子快照。

## 2026-09-18：已提交草稿仍出现在活动列表且旧链接可继续改写

- 现象：草稿版本提交后仍参与草稿分页；旧编辑链接可以把已冻结版本继续保存，造成工作台把已提交内容再次当作活动草稿。
- 原因：草稿查询只按本人／机构／表种分页，没有在分页前排除对应 `SUBMITTED`／`APPROVED` 的 `draft_id + draft_revision`；保存与恢复入口也没有区分历史草稿和当前活动版本。
- 解决：`drafts` 查询在 `LIMIT/OFFSET` 前排除已消耗版本；保存和预览拒绝已冻结版本，旧恢复链接给出明确状态提示；退回单仍可恢复修订。草稿视图恢复时只加载实际存在差异的记录，不再铺开整期正式数据。

## 2026-09-18：跨支行提交详情与历史查询可能泄露其他机构子快照

- 现象：跨支行直接提交使用分行级单据后，支行账号若按单据机构读取会被整单拒绝，若简单放行又会看到另一支行的客户和字段。
- 原因：旧读取逻辑只校验 `submissions.organization_id`，而跨支行单据的机构标记是分行域；列表、详情、审计和记录追溯没有以行级机构再次裁剪。
- 解决：非全行账号按 `submission_items` 关联正式记录筛选可见单据，并对返回快照／审计事件按行级机构过滤；记录追溯先验证目标正式记录，再只返回该支行的快照差异。

## 2026-09-18：删除擅加的期次列，分期只用模板时间

- 明细末尾“期次／历史保留信息”不属于用户模板，与原“时间顺序”重复；删除附加表头和单元格，同步空表 colspan，不删除原模板字段或库内历史附加信息。
- 原 multi/negative 解析在行时间为空时会使用文件名或旧参数，可能把未填时间的记录归错期。现只使用每行时间顺序（有效月份或起止日期），cross 继续只用首次违约日期分月；缺失或无效定位原列并整批报错。
- 新上传参数兼容签名保留，但不使用；月份查询、截止日期、重复识别沿用同一规范化行级时间。原历史迁移兼容逻辑保留，不通过升级自动改写历史归属。
- 表头必须按用户模板，不擅自新增业务展示列。

## 2026-09-18：操作记录域及按月删除

- 操作记录原先分行可见超管，支行账号类别只显示管理员自己。列表、工作流追溯和基础状态事件现同步按事件角色及所属机构限制，避免替代入口绕过。
- 月度删除须覆盖统计、导出、来源去重和旧工作流写回；用新增 V007 标记撤除正式行，保留历史，待复核阻止，旧草稿无法重新提交。
- 清理审计先冻结事件 ID，不能确认时重新执行宽泛条件以免删除后来事件；删除明细与审计链接同事务，保留清理摘要。
- schema 升至 7 后，旧迁移回归的版本数量、模拟未来版本及测试降级表清理须同步更新，不修改原 SQL 迁移。


## 2026-09-18：人员删除入口不明显

- 用户在人员管理列表找不到删除按钮；原入口只在修改页面底部。
- 增加列表的红色“删除人员”链接，进入只读确认页核对目标，POST 复用原停用事务；不做物理删除、不释放认证号、不影响历史记录。
- 已停用账号不重复提供删除按钮；仍可修改状态恢复。保留原跨支行／同级／上级限制、CSRF、版本冲突和待办责任保护。
- 新增 HTTP 测试需查询 `category=security`；页面将 `USER_DISABLE` 显示为“停用人员”，且差异只显示实际变化字段，不能期待未变更的认证号在差异表重复出现。按目标记录 ID 和显示文案验证后，全量离线构建及安装模拟通过。

## 2026-09-17：rc.5 截止日期最早明天

- 用户要求新设／修改日期只能晚于今天；保存事务内按北京时间判断，今天／过去日期拒绝且不写配置和审计；取消与既有日期读取不受影响。
- 原测试用“直接设置过去日期”制造超期，现改为受控时钟下先合法设置未来日期，再推进时间，保留超期、导出与完成状态回归。
- 初次编译发现三参数 PlatformStore 测试构造仅包内可见；增加只接收 Clock 的公共构造，不公开故障注入入口，不新增 HTTP／命令行改时间通道。

## 2026-09-17：rc.4 反馈截止日期独立配置

- 反馈时间不是导入列；按清单和实际来源期次保存，对同一期各支行统一生效。交叉违约仍沿用按首次违约日期归档的月度期次。
- 北京时间截止当天结束前有效；只有正式未完成才标超期，草稿和待复核不计完成；延期或取消后重新计算，不留永久红色状态。
- 新增 V006，保留旧迁移校验和；配置与审计同事务、乐观版本保护，不能改变正式记录版本或使已有提交失效。
- 第一轮回归发现旧健康检查／诊断断言固定为 schema 5；更新至 6，同时更新从 V1/V3/V5 升级和未来版本拒绝测试，不通过修改历史迁移规避。

## 2026-09-17：rc.3 用户变更交叉违约的归档口径

- 最新业务要求覆盖此前按文件月份的规则：L 列“违约首次出现时间”规范化为有效日期，按该日期分月；不使用上传月份或文件名覆盖。
- 无效／空日期定位到 L 列并拒绝整批，不默认为当前月份；兼容文本、Excel 日期数值、有效缓存公式及 1904 日期系统。
- 旧测试关于交叉违约必须补充月份的断言已更新；无新迁移，不在安装升级中自动清库。仅用户本次明确授权的本地交叉违约历史测试记录在校验备份后单独清理，清理工具及数据不入 Git。
- 新增顶部滚动条采用原生 overflow 滚动和 attachEvent 降级；模拟浏览器接口测试不等于实际旧 IE 验收。

## 2026-09-17：修订 2 复审发现四参数 null 角色仍可绕过申请不变量

- 现象：HTTP 层虽已拒绝缺失角色，但平台 `apply(number,name,organization,(Role)null)` 仍可能写入无目标角色待办。
- 原因：四参数入口把 `null` 当作历史兼容值，只有非空角色才校验机构和有效上级；历史行兼容不应延伸到新申请 API。
- 解决：四参数新申请入口先强制拒绝 `null`，再校验机构／角色和上级；历史 `requested_role IS NULL` 仅由迁移后的读取与审批逻辑处理。新增无管理员、有管理员及零待办／通知新增回归。

## 2026-09-17：跨月统一工作簿的公共月份覆盖文件期次

- 现象：多个完整工作簿共用 `month=2026-09` 时，文件名为 2026-10 的交叉违约表（无期次列）被解析成 2026-09，同客户跨月行在暂存去重时折叠丢失。
- 原因：解析器只在公共月份为空时读取文件名月份；交叉违约没有行级期次，公共月份会静默覆盖文件级归属。
- 解决：按行优先使用明确期次；无行期次时使用补充期次或文件名期次／月份；公共月份与文件／补充期次冲突时返回定位错误并整批不暂存。新增真实 HTTP 跨月上传、按月查询／导出及冲突零写入回归。

## 2026-09-17：跨月回归断言误用客户标识和期次表示

- 现象：首次新增跨月 HTTP／解析测试失败，页面实际使用合成 key `cross-month-cross`，且按文件月份推断的 `Period.key()` 是 `YYYY-MM`，测试却断言了另一客户名和日期区间。
- 原因：测试辅助数据与 `Period.parse("", month)` 的月度表示未对齐。
- 解决：统一使用实际合成 key，并分别验证 `2026-09`／`2026-10` 查询与导出各保留一行；修正后完整离线构建通过。

## 2026-09-17：修订 2 迁移版本与旧验收断言不一致

- 现象：新增版本化 V005 后，健康检查和旧迁移测试仍断言 schema 4，离线构建在 HTTP 冒烟阶段失败。
- 原因：反馈字段此前由初始化尾部临时 DDL 添加，测试只覆盖到 V004；改为正式 V005 后诊断值必须真实递增。
- 解决：更新合成测试的 schema 断言及 V3/V1 升级期望为 5，并保留 V001～V004 原脚本不变；再次运行全量离线构建确认。

## 2026-09-17：移除申请兼容哨兵后的旧 HTTP 测试提交缺少角色

- 现象：删除 `__legacy__` 隐藏字段后，旧 AccessHttpTest 的表单辅助函数不再提交 requestedRole，申请返回 400。
- 原因：旧测试模拟三参数申请接口，未模拟真实浏览器的角色 select；修订 2 明确禁止靠隐藏字段绕过角色及上级校验。
- 解决：测试辅助函数按分行/支行提交真实目标角色，并为跨支行场景创建合成支行管理员；页面与 Main 只接受明确合法角色。

## 2026-09-17：最后一名支行管理员停用回归为责任保护

- 现象：HTTP 冒烟测试末尾停用武进最后一名支行管理员时返回 400，旧断言期待 303。
- 原因：修订 2 要求存在待处理权限申请且没有替代管理员时，停用、调机构和改角色均须阻止，测试中的限流前申请正是该责任。
- 解决：保留服务端阻断并把测试改为验证责任保护；后续验收需先处理待办或安排替代管理员再停用。

## 2026-09-17：重复认证号验收从静默收据改为明确拒绝

- 现象：原 AccessPlatformTest 在账号已批准后再次申请同一认证号时未捕获异常，导致新重复提示策略中断测试。
- 原因：修订 2 要求已有账号或待审批占位返回统一、明确的冲突提示，不再伪装成申请已接收。
- 解决：保留服务端无机构细节的 `IllegalArgumentException`，更新合成测试验证拒绝且原申请/账号不变。

## 2026-09-17：密钥丢失测试缺少 IOException 导入

- 现象：新增密钥丢失/备份恢复验收后，IdentityTest 编译器找不到 `IOException`。
- 原因：测试只引入了 `java.nio.file`，未显式引入 `java.io.IOException`。
- 解决：补充 IOException 导入并重新编译；测试仍只操作隔离合成目录。

## 2026-09-17：角色固定后审批测试仍尝试改分配角色

- 现象：申请页已持久化目标角色后，旧审批测试仍把操作员申请批准为支行管理员，触发“申请目标角色已固定”。
- 原因：修订 2 禁止审批人通过表单覆盖申请人的明确角色，旧测试沿用了修订 1 的可变分配假设。
- 解决：审批测试改为批准申请中固定的操作员角色，并保留不同角色复用请求编号的拒绝断言。

## 2026-09-16：统一预览页面错误访问 bundle schema

- 现象：统一导入任务进入“查看逐条明细”时，页面层按单表任务直接调用 `DatasetSchema.get("bundle")`，会抛出数据类型无效。
- 原因：bundle 任务的每一行同时携带真实数据表类型，任务自身不是可直接解析的单表 schema。
- 解决：bundle 默认只渲染汇总；明细模式按每条来源记录的数据表选择 schema，标题使用统一工作簿标签，并增加对应回归测试。

## 2026-09-16：统一导入测试变量遮蔽

- 现象：新增统一导入测试时，`javac` 报 lambda 参数 `r` 与同一测试方法已有变量重名。
- 原因：Java lambda 参数不能遮蔽所在方法作用域内已声明的局部变量。
- 解决：将过滤正式记录的 lambda 参数改为 `record`，恢复测试编译并通过全量离线构建。

## 2026-09-16：统一导入路由的 Part 字段编译错误

- 现象：新增三表统一上传分支后，`javac` 报 `Part` 没有 `filename()` 方法。
- 原因：现有 multipart `Part` 是字段记录，旧代码使用 `file.filename`；新分支误写成了方法调用。
- 解决：统一改为读取 `file.filename` 字段，并重新运行全量离线构建测试确认通过。

## 2026-09-16：反馈角色表单与删除责任回归

- 现象：FEEDBACK 角色下拉加入后，旧 HTTP 兼容测试只提交隐藏字段，申请返回 400；新增停用责任检查后，已有历史待办使正常的支行管理员停用测试被阻止。
- 原因：表单测试使用隐藏字段模拟旧客户端，不能提交新增的 select 值；责任检查把所有历史申请都视为当前管理员的未处理事项，没有区分显式角色申请，也没有先判断是否存在替代管理员。
- 解决：在角色 select 前加入仅供旧客户端兼容的哨兵隐藏值，真实浏览器提交的 select 值会覆盖它；服务端仅对哨兵走原三参数兼容入口。停用检查改为仅在目标管理员是该机构最后一名管理员且存在带明确目标角色的支行待办时阻止，并对最后一名复核员保留待复核保护；支行管理员申请改走分行审批。完整本地测试恢复通过。

## 2026-09-16：反馈功能编译失败

- 现象：执行 `node tools/build.mjs --test --offline` 时，`IdentityPages.java` 报告重复定义 `created(PlatformStore.CreatedUser)`。
- 原因：新增可授权人员查看初始密码页面时，补丁重复保留了同签名的初始密码生成页面方法。
- 解决：删除重复方法，仅保留一个 `created` 页面实现；修复后重新运行本地构建验证。

## 2026-09-16：合并、停用 CI 与离线交付

- 首次源码导出时 Windows tar.exe 不能正确解码 Git tar 中的部分中文文件名。改用 Git ZIP + PowerShell Expand-Archive，逐个核对提交中全部路径和文件数，再补依赖。失败的本地临时输出保留；新的本地输出带提交短号，不删除或覆盖失败目录；U 盘交付名称仍稳定按版本命名。

- 用户明确要求以后不使用 GitHub CI。已停用工作流并移除 `.github/workflows/build.yml`；保留本地全量测试和安装模拟，不能擅自恢复 Actions，也不调整无关权限或保护规则。
- A2 #6 先合并，B2 #7 改 base 为 main 后再合并；两次合并无文件内容冲突。当前统一交付 `0.3.0-rc.1`，schema 4。
- 原打包脚本固定复制 PR0.7 说明，会把已接入的网页误写成尚未开放。改为当前安装升级说明和发布检查，保留历史文档不改写历史测试事实。
- 安装默认目录仍为 ZhihuiXinguan-PR0，避免另建目录导致用户误以为数据丢失；已有账号、密码、业务库在升级时保留，完整旧版备份不自动删除。
- 源码目录用当前 Git 提交导出，不复制整个工作目录；仅补充锁定并校验过的 JAR 缓存，不带真实表、运行库、密码配置或 Git 历史。安装包可显式带本地初始化配置，不能公开上传。

## 2026-09-16：B2 正式读模型与流程提示

- 用户“B2 也做了”表示要求当前维护人开发；此前误解为另一台 AI 已完成，现按后续明确澄清承接 B2，修正协作入口，不重复派发。
- 详情页和授权导出如果分别解析筛选，会漏掉完成状态或翻页条件。统一为 BusinessFilter，正式完成仅判断黄色字段，草稿与待审快照绝不写入 DashboardData 或 ExcelExporter。
- 待审限制是每个操作员／记录，而非全支行唯一。多人对同一记录提交时，操作员优先显示本人待审单；按本人草稿 ID＋版本排除已提交或已批准的版本，不能只与最近一个待审单比较，也不能因后来正式值变化把已批准草稿重新显示成未提交。
- 公共工作流一单只允许一个机构。分行跨支行清单改为正式只读和逐条填写入口，筛选单支行后才提供整页差异预览，不显示必定失败的跨机构批量按钮。
- UI 断言不能用包含“我的草稿”来判断是否存在徽标，操作说明也含该文本；改为精确徽标匹配。搜索关键字会保留在表单中，判断记录排除应匹配完整企业显示文本，不匹配隐藏查询参数。
- 验证仅使用虚构身份、业务记录和工作簿；1766 项断言及安装器模拟通过。1366／1920 的现代浏览器检查不等于真实 Win7 IE 或麒麟验收。

## 2026-09-16：A2 持久化导入与验证边界

- 旧导入预览在 Main 内存中，服务重启即失去任务。改为 V004 本人暂存和选择版本，确认使用同一 H2 事务提交正式记录、导入批次、审计及任务状态，不能嵌套调用会自行 commit 的公开 importRows。
- 预览保存的是正式数据基线，不是授权凭证；确认还必须重验当前账号／身份版本、任务拥有者、过期时间、选择版本及基线。相同重试返回原结果，变更决定重放拒绝。
- 首轮升级测试仍断言 schema 3 和迁移序列 1/2/3；V004 追加后应同步更新期望并保留 V1/V2/V3 升级测试，不改旧迁移文件来迁就测试。
- 缺缓存测试在已有空字符串单元格上调用 setCellFormula，会留下有效的空字符串缓存；它不同于真正无缓存。测试显式 unsetV 构造无缓存，解析器拒绝缺缓存／错误缓存，不把它默认为数值零；数字公式客户编码与数字常量执行相同精度保护。
- 基础状态页面曾硬编码 schema 1，现由 store.schemaVersion() 注入实际值，避免功能已升级但诊断页仍显示旧结构。
- 页面检查和测试仅使用虚构数据。现代浏览器的 1366／1920 视觉检查及安装器模拟测试不代表 Win7 IE／麒麟实机通过。此前“B2 已完成”的理解已由用户澄清更正，见上方 B2 记录。

## 2026-09-15：A1/B1 集成接入与回归

- Git 自动合并保留了两侧开发记录，无需以一侧覆盖另一侧；主要差异是 Main 未接入 B1、通知跳转缺失和 submissionId 审计参数不一致。
- 审计单据筛选必须先检查权限并按 workflow_audit_links 精确过滤，不能忽略未识别参数后显示全部日志。
- 旧批量保存入口如果继续调用 publishDirect 会跳过新流程确认；兼容入口现改为预览，确认后才发布。
- 补充全部草稿分页、复核筛选 if 作用域、退回关联随筛选保留；新增真实 Main 五角色和实际 Excel 导出测试。
- 集成测试曾假设不存在的单据返回 404；公共工作流有意将不存在与越权统一为 403，修正测试期望，未削弱防枚举检查。

## 2026-09-15：A1 回归与页面检查

- 审批创建账号必须复用同一个数据库事务，不能在外层审批事务内调用会自行 commit 的公开 createUser；现拆出同包 helper，并对账号、通知和最终决策阶段注入故障验证回滚。
- 匿名权限申请不可复用 Path=/login 的 CSRF Cookie；独立 ZXAPPLY Cookie 使用 Path=/access/apply，一次性验证，配合来源限流和待办认证号唯一约束。
- A1 新增审计测试曾持有直接修改前的 BusinessRecord 版本，后续保存草稿被正确拒绝；测试改为重新读取正式版本，未放宽生产冲突检查。
- PowerShell 启动 Java 时 `-Dfile.encoding=UTF-8` 必须整体加引号，避免参数被拆分为错误的主类名。
- 本地 Chromium 虚构页面检查发现公共导航的 span 样式覆盖通知气泡间距；增加限定选择器，保持小尺寸红色气泡。视觉检查不等同于 Win7 / IE 实机验收。

## 2026-09-14：PowerShell 文档分段读取失败

- 现象：使用 `[Math]::Min($range[1], $lines.Count)` 控制文档读取范围时，PowerShell 报错 `Argument types do not match`，循环未执行。
- 原因：数组元素与 `Count` 在该表达式中的运行时数值类型不一致，无法匹配同一个 `Math.Min` 重载。
- 解决：先把起止值显式转换为 `[int]`，再调用 `[Math]::Min([int]$end, [int]$lines.Count)`；修正后成功读取目标行。
- 避免复发：涉及 .NET 重载的 PowerShell 数值运算不要依赖隐式类型推断，尤其是值来自异构数组或管道结果时。

## 2026-09-14：Windows 安装器模拟找不到 Bash

- 现象：`node tests/install-smoke.mjs` 报错 `spawnSync C:\Program Files\Git\bin\bash.exe ENOENT`，安装器模拟未开始执行。
- 原因：测试把 Git for Windows 的 Bash 路径写死为 `C:\Program Files\Git\bin\bash.exe`；本机 Git 安装在 `D:\Git`，且 `bash.exe` 未加入 PATH。
- 解决：为测试增加 Bash 解析顺序：`BASH_EXE` 环境变量、PATH 中的 `bash.exe`、根据 `where git.exe` 推导 Git 安装根目录、常见 Program Files 路径。修正后找到 `D:\Git\bin\bash.exe`，安装器模拟完整通过。
- 避免复发：跨机器测试不得假定 Git 或其他工具安装在系统盘固定目录；必须允许显式覆盖并提供可诊断的自动发现与缺失提示。

## 2026-09-14：PowerShell 中的正则扫描表达式损坏

- 现象：在双引号 PowerShell 命令中执行敏感信息正则扫描时，`rg` 报错 `unclosed character class`。
- 原因：正则字符类旁的反引号被 PowerShell 当作转义符处理，传给 `rg` 的表达式已经不是原始文本。
- 解决：对只需确认固定敏感标记的检查改用 `Select-String -SimpleMatch` 和独立字符串列表；修正后新文档扫描通过。
- 避免复发：从 PowerShell 传递包含反引号、美元符号或复杂字符类的正则前必须检查实际参数；固定标记扫描优先使用字面量匹配。

## 2026-09-15：单分支克隆导致远端 main 状态陈旧

- 现象：`git fetch origin --prune` 成功后，`origin/main` 仍停留在初始版本，但 GitHub API 已显示 PR #1、#2、#3 合并。
- 原因：仓库最初用单分支方式克隆，`remote.origin.fetch` 仅包含 `codex/v0.3-foundation`，普通 fetch 不会更新其他远端分支。
- 解决：将 refspec 调整为 `+refs/heads/*:refs/remotes/origin/*` 后重新 fetch，`origin/main` 更新到任务发布提交 `1050fdaa`。
- 避免复发：协作巡检不能只看 fetch 命令是否成功；先核对 `remote.origin.fetch`，再比较 GitHub 远端默认分支与本地 remote-tracking ref。

## 2026-09-15：工作流测试的 Period 类型名称冲突

- 现象：`javac` 报告 `Period` 构造器参数不匹配，同时匹配 `java.time.Period` 与 `xinguan.platform.Period`。
- 原因：测试同时使用 `java.time.*` 和 `xinguan.platform.*` 通配导入，两个包都声明了 `Period`。
- 解决：构造业务期次时显式写为 `new xinguan.platform.Period(...)`，编译恢复。
- 避免复发：Java 文件同时使用多个含同名类型的通配导入时，对领域类型使用完整限定名或改为显式导入。

## 2026-09-15：独立 Java 测试未复制数据库迁移资源

- 现象：工作流路由测试编译成功，但启动 `PlatformStore` 时报 `缺少数据库迁移资源：V001__foundation.sql`。
- 原因：专用测试运行器只编译了 Java 类，没有像正式构建脚本一样把 `resources/` 复制到测试 classpath 根目录。
- 解决：在运行 Java 前执行 `fs.cp(resources, output, {recursive:true})`，V001/V002 可由类加载器读取，真实 H2 测试通过。
- 避免复发：独立 Java 测试必须复刻运行时资源布局；只把依赖 JAR 和 `.class` 放入 classpath 不足以启动依赖类路径资源的模块。

## 2026-09-15：PowerShell 直接启动 Edge 无法可靠取得退出码

- 现象：用调用运算符运行 Edge headless 截图后 `$LASTEXITCODE` 为空，脚本误判为失败且没有生成截图。
- 原因：Windows GUI 可执行文件的直接调用没有在该宿主中提供可依赖的同步退出码语义。
- 解决：改用 `Start-Process -WindowStyle Hidden -Wait -PassThru`，检查进程对象的 `ExitCode`；1366 和 1920 截图均成功生成。
- 避免复发：PowerShell 自动化 GUI 程序时显式等待进程并从 `PassThru` 对象取退出码，不依赖 `$LASTEXITCODE`。

## 2026-09-15：固定测试时钟及随机 ID 导致列表断言不稳定

- 现象：工作流测试首次通过，复跑时“退回原因”或分页内 HTML 转义断言偶发失败。
- 原因：测试时钟固定使多个提交拥有相同创建时间，正式记录及提交再按随机 UUID 排序；测试用列表第一项推断刚创建的提交，并假定特殊客户总在第一页，实际顺序不稳定。
- 解决：从确认结果页面读取服务端返回的稳定提交单 ID，再按 ID 查询精确单据；先定位特殊客户的实际列表下标，再请求对应分页验证转义。
- 避免复发：测试不得依赖同时间或随机 ID 记录的隐式顺序；写操作后使用返回的稳定标识，分页断言先根据真实排序定位目标。

## 2026-09-15：右浮动导致主次按钮视觉顺序反转

- 现象：工作流编辑页 HTML 中先写“保存草稿”再写“提交”，实际截图却显示“提交”在左、“保存草稿”在右，与主操作应位于最右侧的阅读习惯相反。
- 原因：两个按钮都使用 `float:right`，右浮动元素的视觉排列顺序与 DOM 顺序相反。
- 解决：在 DOM 中先输出主按钮“提交”，再输出次按钮“保存草稿”，1366 与 1920 截图确认最终显示为左侧保存、右侧提交。
- 避免复发：旧 IE 兼容布局使用 float 时不能仅按 DOM 顺序判断视觉顺序；必须在目标分辨率截图核对主次动作位置。

## 2026-09-15：确认页面刷新会生成新的请求编号

- 现象：同一服务端预览或同一待审单每次重新渲染时都生成随机 `requestId`，刷新页面后不再沿用原确认动作编号。
- 原因：页面层直接在每次渲染时调用 `UUID.randomUUID()`，没有把请求编号稳定绑定到预览或单据。
- 解决：确认、批准、退回分别使用 `confirm-预览ID`、`approve-单据ID`、`reject-单据ID`；恢复同一预览的测试确认 requestId 保持一致。
- 避免复发：幂等请求编号应绑定服务端业务对象和动作，不应绑定某一次 HTML 渲染；刷新、双击和不确定重试必须复用同一编号。

## 2026-09-15：Git 智能 HTTP 推送持续被连接重置

- 现象：分支内容约 100 KB，普通 `git push` 连续两次报 `Recv failure: Connection was reset`；强制 HTTP/1.1 后连接长期无响应。同期 `gh api` 访问 GitHub 正常。Git Data API 创建的提交与本地提交 tree 完全相同，但提交 SHA 不同。
- 原因：本机到 GitHub 的 Git smart-HTTP 传输链路不稳定，不是提交大小、权限、认证或仓库规则错误；GitHub REST 创建的提交消息末尾没有换行，而本地 `git commit-tree -m` 会补换行，因此 commit 对象 SHA 不同。
- 解决：先逐个通过 GitHub Git Data API 创建并校验 blob，再基于精确基线创建相同 tree、commit 和分支 ref；以 tree SHA 证明文件内容一致，创建远端 ref 后 fetch 该对象并把本地分支指向远端提交。
- 避免复发：推送连接重置时先保留本地提交并有限次重试；若 GitHub API 可用，使用可校验的 Git Data API 传输对象，并同时核对 blob、tree、parent；不要仅凭 commit SHA 不同判断内容偏差，也不要丢弃提交或重复创建 PR。
