# Copy this file to bootstrap.local.properties and set both values locally.
# Used only when the account database is empty. Existing passwords are never reset.
# Keep the populated file private; never commit it or publish a private offline bundle.
auth_number=
password=
